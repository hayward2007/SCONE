\section{플랫폼 및 접촉 모델}

\subsection{기구 및 시뮬레이션 모델}

다리 번호는 전방에서 후방을 바라볼 때 우측이 $1,3,5$, 좌측이 $2,4,6$이다.
액추에이터 ID $1$--$6$은 근위 링크를, $7$--$12$는 중간 링크를, $13$--$18$은
호형 말단을 구동한다. MuJoCo 모델은 57개의 강체, 1개의 부유 기저, 18개의 회전
관절로 구성되며 모델의 총질량은 $4.161\,$kg이다. 알루미늄 프레임과 액추에이터에는
실측 질량을 사용했지만, 15\% 채움률로 출력한 폴리머 부품에는 추정 질량을
사용했다. 따라서 총질량은 재조립한 실물 로봇을 계량하기 전까지 시뮬레이션
파라미터로 보아야 한다. 표~\ref{tab:hardware-ko}는 모델에 사용한 값과 실제로
확인한 부품 값을 구분해 정리한다.

\begin{table}[t]
\caption{시뮬레이션에 사용한 구동·접촉·말단 기하}
\label{tab:hardware-ko}
\centering\footnotesize
\setlength{\tabcolsep}{5pt}
\begin{tabular}{@{}lll@{}}
\toprule
항목 & 값 & 역할\\
\midrule
ID 1--6 & MX-28AT, $77\,$g & 근위\\
ID 7--12 & XM430-W350-T, $82\,$g & 중간\\
ID 13--18 & XM430-W210-T, $82\,$g & 말단 호\\
FR07 / FR12 & $20/46\,$g & 프레임\\
호 반지름 $r_i/r_o$ & $112.5/122.5\,$mm & 내/외\\
호 폭 / 개구 $\psi$ & $44\,$mm / $135^\circ$ & TPU 접촉\\
미끄럼 마찰 $\mu$ & $1.0$ & 호--지면\\
접촉 연성 & $3\,$mm, $\zeta=1$ & \texttt{solimp}/\texttt{solref}\\
모델 질량 & $4.161\,$kg & MuJoCo 전용\\
물리 / 제어 & $500/50\,$Hz & 적분 / 목표\\
\bottomrule
\end{tabular}
\end{table}

각 액추에이터는 이상적인 토크원 대신 위치 피드백을 갖는 전압 입력 DC 모터로
모델링했다. 모터 상수를 $K,R$라 할 때, 외부 PD 제어기와 전압 포화는
\begin{align}
 \tau_i^c &= k_{p,i}(q_i^\star-q_i)+k_{d,i}(\dot q_i^\star-\dot q_i), \label{eq:pd-ko}\\
 V_i &= \operatorname{sat}_{[-V_n,V_n]}
 \left(\frac{R_i}{K_i}\tau_i^c+K_i\dot q_i\right)             \label{eq:dcmotor-ko}
\end{align}
로 표현된다. 각 모터군은 정격 전압, 정지 토크, 무부하 속도의 세 값으로 규정되며,
$K=V_n/\dot q_{\rm nl}$, $R=KV_n/\tau_{\rm stall}$로 유도된다.
값은 $(12\,\mathrm{V},2.5\,\mathrm{N\,m},5.76\,\mathrm{rad/s})$, $(12,4.1,4.82)$,
$(12,3.0,8.06)$이다. 시뮬레이션 이득 $(k_p,k_d)$는 SI 단위로 $(5.73,0.752)$,
$(9.40,0.792)$, $(6.88,0.264)$이다. 적분 이득은 0으로 두었으며, 공급 전압과 정지
토크에 맞춘 포화 한계가 모터 응답을 제한한다.

\subsection{호 접촉과 계단 모서리 역학}

\begin{figure}[t]
\centering\includegraphics[width=\columnwidth]{arc_geometry.pdf}
\caption{말단 링크 하나의 접촉 기하. (a) 개방형 호: 외반지름과 내반지름
$r_o,r_i$, 개구각 $\psi$, 개구 현의 길이 $l_o$를 표시했다. 보행 중에는 호의
최저점 부근 패치가 지지점이 된다. (b) 닫힌 바퀴의 모서리 회전에서 수평 축력
$F_h$와 구동 축토크 $\tau_a$를 구분했다. 수평력만 사용하는 경우에는
$h\to r_o^-$에서 특이점이 생기지만, 이는 구동 바퀴의 보편적인 단높이 한계가
아니다.}
\label{fig:geometry-ko}
\end{figure}

그림~\ref{fig:geometry-ko}에 사용한 기호를 정리했다. 외반지름 $r_o$, 내반지름
$r_i$, 개구각 $\psi$에 대해 개구 현의 길이는
\begin{equation}
 l_o=2r_o\sin(\psi/2)=0.226\ \mathrm{m}                        \label{eq:chord-ko}
\end{equation}
이다. 접촉 모델에는 미끄럼 마찰계수 $\mu=1.0$의 6차원 마찰 원뿔과 $3\,$mm의
연성층을 적용했다. 따라서 TPU 호의 접촉은 연성으로 표현되지만, 호 자체는
변형체로 모델링하지 않았다. 보행 시 지지점은 호의 최저점에서 $0.1\,$mm 이내에
있는 메시 정점들의 중심으로 정의한다. 구름 주행 시에는 이 접촉 패치가 말단
관절의 회전에 따라 호 표면을 이동한다.

호의 중심이 하단 디딤면에서 반지름만큼 높은 곳에 있고, 단높이 $h$인 날카로운
모서리를 넘어가는 상황을 생각하자. 호 중심과 모서리 사이의 수평 거리는
\begin{equation}
 x_e(h)=\sqrt{r_o^2-(r_o-h)^2}=\sqrt{2r_oh-h^2},\quad 0\le h\le 2r_o
                                                               \label{eq:edgearm-ko}
\end{equation}
이다. 호가 하중 $F_i$를 지지한다고 하자. 유리한 방향의 축토크를
$\tau_{a,i}$, 호 중심에 가해지는 전방 수평력을 $F_{h,i}$, 구동 효율을
$\eta\le1$, 안전계수를 $\gamma\ge1$이라 두면 모서리에 대한 준정적 필요
모멘트 조건은
\begin{align}
 \eta\tau_{a,i}+F_{h,i}(r_o-h)&\ge \gamma F_i x_e(h),          \label{eq:stairmoment-ko}\\
 \mu_{\min}&=\frac{|F_t|}{F_n}                                \label{eq:stairfriction-ko}
\end{align}
이다. 축토크가 없는 제한된 경우($\tau_{a,i}=0$, $h<r_o$)에만
\begin{equation}
 F_{h,i}\ge\gamma\frac{F_i x_e(h)}{r_o-h}                     \label{eq:forceonly-ko}
\end{equation}
가 되며, 이 값은 $h\to r_o^-$에서 발산한다. $h\ge r_o$에서는 전방 수평력이
유리한 모멘트를 만들지 못하지만, 식~(\ref{eq:stairmoment-ko})의 축토크 항은
$h<2r_o$에서 유한하다. 따라서 $h=r_o$는 수평력만 사용하는 경우의 특이점이지,
본 연구처럼 말단 관절이 구동되는 닫힌 바퀴의 단높이 한계가 아니다.

개구부와 관절 운동은 두 가지 추가 접촉 수단을 제공한다. 접근 자세가 허용되고
$h<l_o=226\,$mm이면 계단 코가 $135^\circ$ 개구부 안으로 들어와 호의 경계에
반력을 전달할 수 있다. 다만 이 현 길이 조건은 필요한 간극 조건일 뿐, 걸림을
보장하는 충분조건은 아니다. 나머지 관절은 호 전체를 들어 올리고 재배치할 수
있다. 현재 개방형 호 시험은 각 제어기의 성공 범위를 보여주지만, 개구부의 효과를
분리하려면 질량·반지름·토크·제어 조건을 맞춘 닫힌 바퀴 형상과 직접 비교해야
한다. 이 절제 실험은 \ref{sec:method-ko}절에서 정의한다. 개구 현과 한 계단의
대각선 길이의 비 $l_o/\sqrt{h^2+d^2}$는 세 형상에서 각각 $0.87$, $0.91$,
$0.56$이며, 이 값만으로 관측 결과의 순서를 설명할 수는 없다.
